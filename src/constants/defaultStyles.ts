import { StyleSheet } from "react-native";

export const defaultStyles = StyleSheet.create({
  image: {
    width: "100%",
    height: "100%",
    objectFit: "contain",
  },
  itemName: {
    fontWeight: "bold",
    fontSize: 16,
  },
  subText: {},
});
